{
  "name": "vivawav3",
  "private": true,
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n fe,api -c cyan,magenta \"npm run dev -w frontend\" \"npm run dev -w server\"",
    "build": "npm run build -w frontend && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/material": "^9.0.0"
  }
}
